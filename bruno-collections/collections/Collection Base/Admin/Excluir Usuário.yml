info:
  name: Excluir Usuário
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseURL}}/admin/users/{{id_user_teste}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
