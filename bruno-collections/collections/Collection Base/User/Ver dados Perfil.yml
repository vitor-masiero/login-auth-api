info:
  name: Ver dados Perfil
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/user/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
